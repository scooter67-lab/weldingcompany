node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.env*
.DS_Store

# Рабочая копия ветки deploy (создаётся скриптом выкладки)
.deploy/

# Исходные материалы заказчика (презентация, логотип, фото).
# Репозиторий публичный, поэтому в него не попадают.
# Нужны только для повторного запуска скриптов подготовки картинок;
# готовые ассеты уже лежат в public/images и версионируются.
docx/
