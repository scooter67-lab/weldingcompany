/**
 * Счётчики аналитики и подтверждение прав в вебмастерах.
 *
 * Все значения ниже — публичные идентификаторы, они и так видны в коде
 * страницы. Секретов здесь нет, поэтому держим их в обычном файле, а не
 * в переменных окружения: сборка делается локально, и лишний .env только
 * усложнил бы выкладку.
 *
 * Пока строка пустая — соответствующий скрипт или мета-тег не выводится
 * вообще. Так сайт не тащит лишний вес, пока счётчик не заведён.
 *
 * Где взять значения — см. deploy/SEO.md
 */

/** Счётчики посещаемости. */
export const analytics = {
  /** Номер счётчика Яндекс.Метрики, только цифры: "98765432" */
  yandexMetrica: "",
  /** Идентификатор потока Google Analytics 4: "G-XXXXXXXXXX" */
  googleAnalytics: "",
} as const;

/**
 * Коды подтверждения прав на сайт.
 * Нужны, только если подтверждаете мета-тегом. При подтверждении
 * через DNS-запись TXT эти поля остаются пустыми.
 */
export const verification = {
  /** Google Search Console, значение content= из мета-тега */
  google: "",
  /** Яндекс.Вебмастер, значение content= из мета-тега */
  yandex: "",
} as const;

/**
 * Запись Метрики фиксируется только на боевом домене: иначе локальные
 * прогоны и тестовые сборки портят статистику.
 */
export const isProductionHost = (host: string) => host === "weldingcompany.kz";
