# Нормализация переводов строк: в репозитории всегда LF
* text=auto

# Конфиг Apache и скрипты выкладываются на Linux-сервер — только LF
.htaccess text eol=lf
*.conf text eol=lf
*.sh text eol=lf

# Бинарные файлы не трогать
*.webp binary
*.png binary
*.jpg binary
*.pdf binary
*.woff2 binary
